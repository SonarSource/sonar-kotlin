name: Releasability Status
on:
  workflow_dispatch:
  workflow_run:
    workflows: [CI]  # Name must match the build workflow
    types:
      - completed
    branches:
      - master
      - branch-*

jobs:
  releasability-status:
    name: Releasability status
    runs-on: github-ubuntu-latest-s
    permissions:
      id-token: write
      statuses: write
      contents: read
    steps:
      - uses: SonarSource/gh-action_releasability/releasability-status@v3
        env:
          GITHUB_TOKEN: ${{ github.token }}
